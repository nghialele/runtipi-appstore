version: "3.9"

networks:
  tipi_main_network:
    name: runtipi_tipi_main_network
    external: true
    driver: bridge
    ipam:
      config:
        - subnet: 172.19.0.0/16
          gateway: 172.19.0.1
